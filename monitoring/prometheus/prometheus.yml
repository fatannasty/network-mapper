global:
  scrape_interval: 30s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: network-mapper
    scrape_interval: 30s
    static_configs:
      - targets: ["host.docker.internal:8000"]
