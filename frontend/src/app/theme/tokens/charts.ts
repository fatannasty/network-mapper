/** Chart colour series references driven by CSS variables so Recharts /
    chart libraries adapt to the active theme automatically. */
export const chartTokens = {
  series1: 'rgb(var(--chart-1))',
  series2: 'rgb(var(--chart-2))',
  series3: 'rgb(var(--chart-3))',
  series4: 'rgb(var(--chart-4))',
  series5: 'rgb(var(--chart-5))',
  series6: 'rgb(var(--chart-6))',
  series7: 'rgb(var(--chart-7))',
  series8: 'rgb(var(--chart-8))',
} as const
