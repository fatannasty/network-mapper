services:
  catalyst-agent:
    build:
      context: .
      dockerfile: Dockerfile.agent
    container_name: network-mapper-agent
    network_mode: host
    restart: unless-stopped
    env_file:
      - .env.agent
